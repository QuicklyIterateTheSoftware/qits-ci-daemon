<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Standalone parent for qits-ci-daemon, deliberately the same shape as
         qits-workspace-daemon's root: the eu.wohlben:qits:1.0.0-SNAPSHOT coordinates a child's
         <parent> points at (../pom.xml), the quarkus-bom import, and the compiler/surefire pins —
         and nothing else. The app-wide machinery of the monorepo (spotless, dev-guard, enforcer,
         jandex) is intentionally absent; a clone of this repo alone must `./mvnw verify` green.

         The reactor holds the wire contract and the application that speaks it. Both exist; a
         module listed here that did not would break the clone-alone rule this file is here to
         keep. -->
    <groupId>eu.wohlben</groupId>
    <artifactId>qits</artifactId>
    <version>2026.803.184200</version>
    <packaging>pom</packaging>

    <modules>
        <module>ci-daemon-protocol</module>
        <module>ci-daemon</module>
    </modules>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.34.6</quarkus.platform.version>

        <compiler-plugin.version>3.15.0</compiler-plugin.version>
        <surefire-plugin.version>3.5.4</surefire-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Supplies the managed versions the children leave unversioned: junit-jupiter for
                 both modules, quarkus-arc/quarkus-vertx for the application. The protocol module
                 has no main-scope dependency at all and is not allowed to grow one. -->
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- The wire-contract module, so a version-less dependency on it resolves within the
                 reactor once the application module exists. -->
            <dependency>
                <groupId>eu.wohlben</groupId>
                <artifactId>ci-daemon-protocol</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>${quarkus.platform.group-id}</groupId>
                    <artifactId>quarkus-maven-plugin</artifactId>
                    <version>${quarkus.platform.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler-plugin.version}</version>
                    <configuration>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
