# THE ONE QA PIPELINE THIS REPOSITORY HAS, and it runs for a release request and nothing else.
#
# There is no push pipeline any more, and `ci-post-receive.yml` is what this file replaces.
# qits-projects keeps a backing branch `release/<id>` per open release request — an octopus merge of
# that request's sources, refolded whenever the set changes — and publishes `ReleaseRequestChanged`
# on every successful re-fold. The merge primitive that writes that branch fires no `post-receive`
# and so publishes no `SCMPublishCommit`: without this file the fold exists and NOTHING BUILDS IT.
# A fold the git host answered `unchanged`, and one that conflicted, publish nothing — so every
# arrival here is a new sha worth a build.
#
# DECIDED AT MAIN, BUILT AT THE FOLD. This file is discovered, parsed and selected at `main`'s head,
# always; only the run's branch and sha come from the payload, which is why `checkout:` names them
# as a pair — a sha with no branch whose history holds it is not something a step container can
# fetch. A release request therefore cannot alter the CI that gates it by folding in a commit that
# edits this file. Inside a step, $QITS_CI_BRANCH is `release/<id>` and $QITS_CI_SHA is the merged
# sha; the fold is already checked out, so nothing below fetches or switches refs.
#
# EVERY STEP HERE IS GATING, and there is no trailing `gating: false` half because this repository
# publishes nothing from a fold. The verdict returns on the ordinary `BuildSuccessful`/`BuildFailed`
# with `commitSha` = the merged sha, which is what qits-projects' `BuildStatusListener` matches on
# together with `repoId`.
#
# ci-event-release.yml BESIDE THIS FILE IS UNTOUCHED and is still the other half: it reacts to this
# repository's release TAG and publishes the released coordinate. This file names a build by the
# merged sha; that one names a release by its version. Neither pushes the other's coordinate.
#
# Two steps, and the split is the repo's own two rules (CLAUDE.md):
#
#   1. `./mvnw verify` — "a clone of this repo alone builds and tests green". No docker, no
#      network beyond Maven Central, no prior install anywhere. It runs FIRST because it is the
#      cheap gate: a broken test must not cost a GraalVM native build.
#   2. the shipping form — a fully static musl native image. A sibling service proves its native
#      build inside `docker build` (its Dockerfile has a native stage); this repo ships no image
#      at all, so the native build is the artifact itself and needs its own step.
#
# --- WHY STEP 2 BUILDS INSIDE A CONTAINER IT STARTS BY HAND ---------------------------------------
# The documented recipe (README.md, "Building the binary") is `./mvnw -Dnative` with
# `quarkus.native.container-build=true`, which makes Maven `docker run` the builder image with the
# project bind-mounted. That does not work from a step container: `docker: true` hands this step
# the HOST daemon's socket, so every `-v` path is resolved on the host and the step's own
# /workspace does not exist there. `docker build` is fine (the CLI streams its context over the
# API) and so is `docker cp` (same), which is the whole trick — source in, binary out, and
# container-build switched off because we are already inside the container it would have launched.
# qits-local-up.sh builds the daemon exactly this way and for exactly this reason; the two should
# keep matching.
#
# The native build's ~4 GB therefore belongs to the HOST daemon, not to this step's container —
# the same reading every sibling's `docker: true` build step has of its own memory.

event: ReleaseRequestChanged

when:
  # repoName, not repoId: the git host's storage coordinate is an opaque UUID since
  # the identity cutover, so a name match on repoId is not dependable. The event
  # echoes the public name; a rename updates this line in the same campaign.
  - repoName: { exact: qits-ci-daemon }

checkout:
  branch: backingBranch
  sha: mergedSha

steps:
  - image: qits/build-images/maven-base:latest
    timeout-seconds: 1800
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this a failed build is swallowed
      # and the run reports SUCCESS.
      set -eu
      # Maven Central through the platform mirror's in-network alias — empty (an older qits-ci that
      # injects nothing, or a deployment that turned the mirror plane off) deactivates the settings
      # profile and Central is resolved directly.
      export QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_PROXY_URL:-}"
      ./mvnw -B -ntp -s .qits-maven-settings.xml verify

  - image: qits/build-images/ci-base:latest
    docker: true
    timeout-seconds: 3600
    script: |
      set -eu

      # The builder image lives in NO registry (README.md: "built locally and lives in no
      # registry"), so whatever needs it builds it. Its own FROM names qits-platform-mirror's
      # pull-through mirror at localhost:8082 — an address the HOST daemon resolves, which is the
      # side that runs this build. `quarkus.native.builder-image.pull=missing` in the app's
      # application.properties is what stops the native build from trying to `docker pull` it.
      #
      # --network host for the same reason every fleet image build carries it: the toolchain
      # tarballs the Dockerfile curls now come from registry.dev.localhost, an edge vhost published
      # on the host's 127.0.0.1:8080. A RUN in the default bridge namespace resolves that name to
      # its own loopback and dials nothing. It applies to the RUN steps only — the FROM was always
      # the daemon's own resolve and never needed it.
      docker build --network host \
        -t qits/graalvmce-musl-builder:jdk-25 -f docker/Dockerfile.musl-builder docker/

      # --entrypoint: the builder image entrypoints to native-image itself. --user root: the base
      # runs as 1001 and /qits-build arrives owned by root.
      # --network host and QITS_MAVEN_CENTRAL_URL route the builder's Maven Central resolve through
      # the mirror, exactly like the fleet's docker-build RUNs: the injected URL is an edge vhost on
      # 127.0.0.1:8080, which only the host namespace reaches. Empty deactivates the settings
      # profile and the resolve goes to Central directly, as it always did. No credential rides
      # along — edge reads are anonymous, and a transient container's env is `docker inspect`able;
      # if the vhost read policy ever tightens, this build fails loudly and earns a secret then.
      # The ldd assertion is inside the build container because that is where the binary is, and it
      # is `ldd` rather than `file` on purpose: GraalVM emits a position-independent static
      # executable, so `file` says "static-pie linked" and never the literal "statically linked".
      cid=$(docker create --user root --network host --entrypoint bash \
        -e QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_CENTRAL_MIRROR_URL:-}" \
        qits/graalvmce-musl-builder:jdk-25 -c '
        set -eu
        cd /qits-build
        ./mvnw -B -ntp -s .qits-maven-settings.xml -pl ci-daemon -am package -Dnative -DskipTests \
          -Dquarkus.native.container-build=false
        ldd ci-daemon/target/qits-ci-daemon 2>&1 | grep -qE "statically linked|not a dynamic executable"
      ')
      docker cp "$PWD" "$cid:/qits-build"
      docker start -a "$cid" || { docker rm -f "$cid" >/dev/null; echo "native build failed" >&2; exit 1; }
      docker cp "$cid:/qits-build/ci-daemon/target/qits-ci-daemon" ./qits-ci-daemon
      docker rm "$cid" >/dev/null

      # The cheap check the test suite structurally cannot do (README.md): a binary run with no
      # environment at all must name the first missing variable and exit 2, not die resolving
      # config. An empty `defaultValue` regresses exactly this and nothing in `./mvnw verify` sees
      # it. alpine:3 also proves the musl half — a glibc-linked binary answers "no such file or
      # directory" here rather than exiting 2.
      probe=$(docker create alpine:3 /qits-ci-daemon)
      docker cp ./qits-ci-daemon "$probe:/qits-ci-daemon"
      docker start -a "$probe" || true
      code=$(docker inspect -f '{{.State.ExitCode}}' "$probe")
      docker rm "$probe" >/dev/null
      [ "$code" = 2 ] || { echo "a daemon run with no environment exited $code, expected 2" >&2; exit 1; }

      echo "qits-ci-daemon built: $(sha256sum ./qits-ci-daemon | cut -d' ' -f1)"
