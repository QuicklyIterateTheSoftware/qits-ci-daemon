# Build outputs
target/
target-ide/

# Where docker/Dockerfile's `--output type=local,dest=out` drops the binary and its SBOM
out/

# IDE
.vscode/
.idea/
.project
.classpath
.factorypath
.settings/
*.iml

# OS
.DS_Store

# Home-relative caches a tool leaks into the repo root when run with the repo as its cwd
.cache/
.m2/
.local/
